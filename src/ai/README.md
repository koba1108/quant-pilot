# AI Investment Committee

Strategy C will live here.

Planned roles:
- Portfolio Manager
- Macro Analyst
- Risk / Critic

The AI layer must consume validated, timestamped market/research inputs. It must not calculate deterministic portfolio constraints that belong in `portfolio/`.
