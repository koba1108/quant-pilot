{
  "name": "ai-etf-system",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "test:node": "node --experimental-strip-types --test tests/*.test.ts",
    "backtest": "bun run src/backtest/runner.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  },
  "engines": {
    "node": ">=26.7.0"
  }
}
