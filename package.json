{
  "name": "quant-pilot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "test:node": "node --experimental-strip-types --test tests/*.test.ts",
    "backtest": "bun run src/backtest/runner.ts",
    "data-quality": "bun run src/data/data-quality-runner.ts",
    "provider-evaluation": "bun run src/data/provider-evaluation-runner.ts",
    "robustness": "bun run src/backtest/robustness-grid.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  },
  "engines": {
    "node": ">=26.7.0"
  }
}
